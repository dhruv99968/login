new added branch 

Push Command :-

push :
	upload local repo content to remote repo
	"git push origin main"

Init Commmand :-
	used to create a new repository
	git init
	git remote add orgin <- link ->
	git remote -v	(to verify remote)
	git branch	(to check branch)
	git branch -M main	(tg rename branch)
	git push origin main
	git push -u origin main -> short form for origin main


mkdir -> make directory

Git branches :- merge,
	git branch  -> To check branch
	git branch -M main -> To rename branch
	git checkout <- branch name -> (to navigate)
	git checkout -b <- new branch name -> (to create new branch)
	git branch -d <- branch name -> (to delete branch)

Merging Code :-
Way 1 :-
	git diff <- branch name -> to compare commits,branches ,files & more
	git merge <- branch name -> to merge 2 branches

Resolving Merge Condlicts :-
	An event that takes place when Git is unable to automatically reslove differences in code between two commits.


How to add project in github using command?
-->